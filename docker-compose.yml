version: "3.9"
services:
  app:
    container_name: pokemon-app
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./app:/usr/src/app
    command: sh -c "npm run dev -- --host 0.0.0.0"
    ports:
      - 5173:5173
    stdin_open: true
